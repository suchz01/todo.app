{
  "name": "todo.app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ins": "npm install && concurrently \"cd frontend && npm install\" \"cd backend && npm install\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7"
  }
}
